package com.caophuc.notification.service;

public interface NotificationService {
    // Hiện tại toàn bộ logic hứng Event đã được chuyển sang NotificationListener
    // Nếu bạn có các API liên quan đến thao tác dữ liệu nội bộ của Notification (ví dụ gửi tự động theo batch) 
    // thì có thể thêm vào đây.
}
